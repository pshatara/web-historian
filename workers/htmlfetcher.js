var archive = require('../helpers/archive-helpers');
var server = require('../web/basic-server');

// if ()
  // if not archived
    // download file
    //router[websiteurl] = archivehandler.handleRequest
